import { FertilizationTypePipe } from './fertilization-type.pipe';

describe('FertilizationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FertilizationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
