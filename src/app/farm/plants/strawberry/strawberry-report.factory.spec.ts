import { StrawberryReportFactory } from './strawberry-report.factory';

describe('StrawberryReportFactory', () => {
  it('should create an instance', () => {
    expect(new StrawberryReportFactory()).toBeTruthy();
  });
});
