import { TreeReportYieldPipe } from './tree-report-yield.pipe';

describe('TreeReportYieldPipe', () => {
  it('create an instance', () => {
    const pipe = new TreeReportYieldPipe();
    expect(pipe).toBeTruthy();
  });
});
