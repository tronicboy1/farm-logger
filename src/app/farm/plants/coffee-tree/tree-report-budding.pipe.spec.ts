import { TreeReportBuddingPipe } from './tree-report-budding.pipe';

describe('TreeReportBuddingPipe', () => {
  it('create an instance', () => {
    const pipe = new TreeReportBuddingPipe();
    expect(pipe).toBeTruthy();
  });
});
