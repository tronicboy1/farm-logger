import { PlantReportFactory } from './plant-report.factory';

describe('PlantReportFactory', () => {
  it('should create an instance', () => {
    expect(new PlantReportFactory()).toBeTruthy();
  });
});
