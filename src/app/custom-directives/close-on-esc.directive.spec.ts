import { CloseOnEscDirective } from './close-on-esc.directive';

describe('CloseOnEscDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnEscDirective();
    expect(directive).toBeTruthy();
  });
});
