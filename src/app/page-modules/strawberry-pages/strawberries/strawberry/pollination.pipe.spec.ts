import { PollinationPipe } from './pollination.pipe';

describe('PollinationPipe', () => {
  it('create an instance', () => {
    const pipe = new PollinationPipe();
    expect(pipe).toBeTruthy();
  });
});
